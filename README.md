# Cafeína vs “Mateína” ☕🧉  
**Misma molécula. Diferente planta. Diferente contexto. Diferente relato.**

Este repo es una mini-demo educativa que muestra, de forma **lógica y ejecutable**, que:

- **“Mateína” y cafeína son la misma molécula** (principalmente: **1,3,7-trimetilxantina**, **C8H10N4O2**).
- La **“diferencia”** que la gente siente (o describe) suele venir del **origen vegetal**, la **matriz de consumo** (dosis/ritual/tiempo), **co-compuestos**, y la **segmentación cultural** (memética) del nombre.

Incluye un script que imprime la demostración en consola y **muestra la estructura molecular** con una imagen.

---

## ¿Qué hay en el repo?

- `mateina_vs_cafeina.py`  
  Script principal. Imprime:
  - Veredicto químico (identidad molecular)
  - “Diferencia” contextual (planta + consumo + co-compuestos)
  - Segmentación “darwiniana” (memética): por qué sobrevive la etiqueta “mateína”
  - Muestra la imagen de la estructura.

- `caffeine_structure.png`  
  Imagen de la estructura (cafeína / “mateína”).

- `requirements.txt`  
  Dependencias mínimas (matplotlib para mostrar la imagen).

- `run_all.bat`  
  Ejecutable “todo en uno” (Windows):
  - crea `.venv`
  - instala dependencias
  - corre el script y muestra el output + abre la imagen

---

## TL;DR (conclusión)

✅ **Químicamente**: “mateína” = **cafeína** (misma molécula principal)  
🧉☕ **Prácticamente**: cambia el “pack” → planta + forma de consumo + co-compuestos + expectativas/relato cultural

---

## Quickstart (Windows)

1) Cloná el repo o descargalo como ZIP  
2) Doble click a:

```bat
run_all.bat
